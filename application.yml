eureka:
  client:
    instance:
      serverUrl:
        defaultZone: http://localhost:8761/eureka/
spring:
  application:
    name: QUESTION-SERVICE-123
  jpa:
  hibernate:
    ddl-auto: update
  show-sql: true
  database-platform: org.hibernate.dialect.PostgreSQLDialect
